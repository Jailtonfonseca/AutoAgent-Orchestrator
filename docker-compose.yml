services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SERVER_SECRET_KEY=${SERVER_SECRET_KEY:-default_secret_key_change_me}
    restart: always
